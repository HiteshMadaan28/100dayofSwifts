import Cocoa

//Ternary Operator

let age=18

let canVote = age>=18 ? "Yes" : "NO"

print(canVote)

let hour=23
print(hour<12 ? "Its afterNOo" : "NightTime")

var a:[Int]=[]

print(a.count==0 ? "Empty":"Not Empty")
